{
  "name": "jaunvardi",
  "version": "1.0.0",
  "description": "A dictionary that uses firebase databse to store items",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Reinards",
  "license": "ISC",
  "dependencies": {
    "firebase": "^5.7.3"
  }
}
